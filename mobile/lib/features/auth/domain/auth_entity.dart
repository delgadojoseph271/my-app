/// Entidad del dominio Auth.
/// Clase Dart pura — sin imports de Flutter, Dio ni ninguna librería externa.
/// Solo lógica de negocio.
class AuthEntity {
  final int id;
  // Agregá los campos propios de la entidad

  const AuthEntity({
    required this.id,
  });

  // Lógica de negocio pura (si aplica)
  // bool get isValid => ...
}
